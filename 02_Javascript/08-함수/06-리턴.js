function getTimes(x, y) {
  const z = x * y;
  return z;
}

const a = getTimes(9, 2);
console.log(a);

const b = getTimes(9, 2) * 10;
console.log(b);

console.log(getTimes(100, 20));

// console.log는 출력하는 함수이다.